/**
 * Category repository.
 *
 * Medusa v2 uses MedusaService which generates repository methods automatically
 * from the model definition. Custom query methods that cannot be expressed via
 * the auto-generated API (e.g. complex recursive CTE queries) should be added
 * here and injected into CategoryModuleService.
 *
 * For now the default MikroORM EntityRepository provided by MedusaService is
 * sufficient. Add custom methods here as the data access requirements grow.
 */
export {}
